# Database (MySQL 8+)
# Local dev:
#   mysql://mahamordo:mahamordo_pass@127.0.0.1:3306/mahamordo
# Hostinger Business (Node.js Application + MySQL Databases):
#   mysql://USER:PASSWORD@HOST:3306/DBNAME
DATABASE_URL="mysql://mahamordo:mahamordo_pass@127.0.0.1:3306/mahamordo"

# Shadow DB — used by `prisma migrate dev` only (never by `migrate deploy`).
# Skip this on shared hosting where the app user can't CREATE DATABASE.
# Local dev: provision once: CREATE DATABASE mahamordo_shadow;
#   GRANT ALL ON mahamordo_shadow.* TO 'mahamordo'@'localhost';
SHADOW_DATABASE_URL="mysql://mahamordo:mahamordo_pass@127.0.0.1:3306/mahamordo_shadow"

# Anthropic Claude API
ANTHROPIC_API_KEY="sk-ant-..."
# Optional model overrides (defaults: fast=claude-haiku-4-5-20251001)
# ANTHROPIC_FAST_MODEL="claude-haiku-4-5-20251001"
# ANTHROPIC_MODEL="claude-haiku-4-5-20251001"

# OpenAI — fortune post AI images (DALL-E 3)
OPENAI_API_KEY="sk-..."

# JWT Auth (generate secret: openssl rand -base64 32)
JWT_SECRET="your-secret-here"
JWT_ACCESS_EXPIRES="15m"
JWT_REFRESH_EXPIRES_DAYS="7"

# App URL (no trailing slash)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Google OAuth
# 1. ไปที่ https://console.cloud.google.com/apis/credentials
# 2. สร้าง OAuth 2.0 Client ID (Web application)
# 3. เพิ่ม Authorized redirect URI: http://localhost:3000/api/auth/google/callback
GOOGLE_CLIENT_ID="xxx.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-xxx"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"

# Apple Sign-In — DISABLED (see docs/REMAINING.md, src/lib/auth-providers.ts)
# Do not add APPLE_* env vars until Apple login is explicitly re-enabled.

# Stripe (credits / subscription)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Resend (password reset email in production)
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="noreply@mahamordo.app"

# Uploads — fortune post + article cover images on server disk (see docs/DEPLOY_STORAGE.md)
# Dev default: data/uploads (no env needed)
# Production (DigitalOcean VPS): UPLOAD_DIR=/var/lib/mahamordo/uploads
UPLOAD_DIR="data/uploads"

# Articles — daily AI cron secret (used by /api/cron/articles/daily)
# Generate: openssl rand -hex 32
# Call from system cron / external pinger:
#   curl -H "Authorization: Bearer $CRON_SECRET" https://your-domain/api/cron/articles/daily
# Enable in CMS at /cms/articles/settings (articles_cron_enabled = true)
CRON_SECRET="set-a-long-random-string-here"

# Public site URL — used by metadataBase / sitemap.xml / robots.txt / JSON-LD.
# Must be the exact production origin (no trailing slash). Falls back to
# NEXT_PUBLIC_APP_URL, then https://mahamordo.com if both are unset.
NEXT_PUBLIC_SITE_URL="https://mahamordo.com"

# Search engine ownership verification (all optional — leave blank to skip).
# Get tokens from each console using the "HTML tag" verification method,
# then paste only the `content="..."` value here.
#   Google:  https://search.google.com/search-console  → meta name="google-site-verification"
#   Bing:    https://www.bing.com/webmasters           → meta name="msvalidate.01"
#   Yandex:  https://webmaster.yandex.com              → meta name="yandex-verification"
GOOGLE_SITE_VERIFICATION=""
BING_SITE_VERIFICATION=""
YANDEX_VERIFICATION=""
